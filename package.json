{
  "name": "gwt-context-doc-task",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pytest && python -m tests.benchmarks.regression_smoke",
    "benchmark:smoke": "python -m tests.benchmarks.regression_smoke",
    "release:gate": "python scripts/release_gate.py",
    "qwen:sanity": "python scripts/qwen_sanity.py",
    "memory:clear": "python scripts/clear_codex_memory.py",
    "memory:bootstrap": "python scripts/codex_mcp_bootstrap.py",
    "memory:health": "python scripts/codex_mcp_health.py"
  }
}
