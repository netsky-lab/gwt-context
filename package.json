{
  "name": "gwt-context-doc-task",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pytest && python -m tests.benchmarks.regression_smoke",
    "benchmark:smoke": "python -m tests.benchmarks.regression_smoke"
  }
}
